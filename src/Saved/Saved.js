import "./Saved.css";

export const Saved = () => {
  return (
    <section>
      //placeholder for ParkChecklist component with only the saved values pertaining to the logged in user
    </section>
  )
}